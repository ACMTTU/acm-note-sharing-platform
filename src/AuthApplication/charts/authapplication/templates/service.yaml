apiVersion: v1
kind: Service
metadata:
  name: {{ template "authapplication.fullname" . }}
  labels:
    app: {{ template "authapplication.name" . }}
    chart: {{ template "authapplication.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "authapplication.name" . }}
    release: {{ .Release.Name }}
