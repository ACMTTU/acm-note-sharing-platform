apiVersion: v1
kind: Service
metadata:
  name: {{ template "notesapplication.fullname" . }}
  labels:
    app: {{ template "notesapplication.name" . }}
    chart: {{ template "notesapplication.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "notesapplication.name" . }}
    release: {{ .Release.Name }}
